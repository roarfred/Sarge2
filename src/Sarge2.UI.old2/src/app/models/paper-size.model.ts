export class PaperSize {
    constructor(name:string, width:number, height:number) {
        this.name = name;
        this.width = width;
        this.height = height;
    };

    name: string;
    width: number;
    height: number;
}